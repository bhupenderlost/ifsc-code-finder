<%- include('includes/header.ejs') %>
<div class="content container-fluid">
    <!--IFSC CODE FORM START-->
    <div class="container-fluid forms">
        <% if (pageTitle.length === 0) { %>
        <h1>404 - Page Not Found</h1>
        <script>document.title = "404 - Page Not Found";</script>
        <% } else { %>
        <div class="container-fuild find-ifsc">
            <div class="container-fluid header-text">
                <h1>Find IFSC Code</h1>
                <p>Find Your Bank's IFSC Code</p>
            </div>
            <form class="form-inline justify-content-center">
                <div class="form-group">
                    <label for="bank">Bank:</label>
                    <select id="bank" class="form-control" onchange="this.options[this.selectedIndex].value && (window.location = this.options[this.selectedIndex].value);">
                        <option>Select Your Bank</option>
                        <% for (let bank of banks) { %>
                        <option value="/<%= bank.bankName.replace(/\s/g, '-').toLowerCase() %>"><%= bank.bankName %></option>
                        <% } %>
                    </select>
                </div>
        
                <div class="form-group">
                    <label for="state">State:</label>
                    <select id="state" class="form-control">
                        <option>Select Your State</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="district">District:</label>
                    <select id="district" class="form-control">
                        <option>Select Your District</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="branch">Branch:</label>
                    <select id="branch" class="form-control">
                        <option>Select Your Branch</option>
                    </select>
                </div>
            </form>
        </div>
       
            <% } %>
    </div>
    <!--IFSC CODE FORM END-->
    <div class="main-content">
        <div class="row">
            <div class="col-md-8 content">
               
                <hr>
                <h1 class="blog-title">
                    <%= pageTitle %>
                </h1>

                <img class="img-thumbnail" alt="<%= pageTitle %>" src="<%= pageImage %>" />
                <hr>
                    <p><%- pageContent %></p>
                    <h3 class="text-center h-6">Thank You For Giving Your Valuable Time!</h3>
                <hr class="end">
                <h2>More Information: </h2>
                <%- include('includes/main.ejs') %>

            </div>
            <%- include('includes/sidebar.ejs') %>
        </div>
    </div>
    
</div>
<%- include('includes/footer.ejs') %>